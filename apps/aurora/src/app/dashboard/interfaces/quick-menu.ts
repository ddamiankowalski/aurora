export type QuickMenuItems = QuickMenuItem[];

export interface QuickMenuItem {
  icon: string;
}

export type QuickMenuActions = QuickMenuAction[];

export interface QuickMenuAction {
  icon: string;
  name: string;
}
